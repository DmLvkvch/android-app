<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.example.ricknmortyapp.model.entity.character.CharacterGender" />

        <import type="com.example.ricknmortyapp.model.entity.character.CharacterStatus" />

        <variable
            name="character"
            type="com.example.ricknmortyapp.model.entity.character.Character" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardElevation="8dp"
        tools:context=".ui.character.CharacterFragment">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <ImageView
                android:id="@+id/character_image"
                android:layout_width="200dp"
                android:layout_height="200dp"
                android:layout_alignParentTop="true"
                android:layout_marginEnd="16dp"
                app:url="@{character.image}"/>

            <TextView
                android:id="@+id/character_name"
                android:layout_width="match_parent"
                android:layout_height="20dp"
                android:layout_marginTop="20dp"
                android:layout_toEndOf="@id/character_image"
                android:text="@{character.name}" />


            <TextView
                android:id="@+id/character_gender"
                android:layout_width="match_parent"
                android:layout_height="20dp"
                android:layout_below="@+id/character_name"
                android:layout_marginTop="10dp"
                android:layout_toEndOf="@id/character_image"
                android:text="@{character.gender}" />

            <TextView
                android:id="@+id/character_status"
                android:layout_width="match_parent"
                android:layout_height="20dp"
                android:layout_below="@+id/character_gender"
                android:layout_marginTop="10dp"
                android:layout_toEndOf="@id/character_image"
                android:text="@{character.status}" />
        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</layout>